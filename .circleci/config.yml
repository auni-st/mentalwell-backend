version: 2.1

jobs:
  # build:
  #   docker:
  #     - image: circleci/node:16
  #   steps:
  #     - checkout
  #     - run:
  #         name: Install Dependencies
  #         command: sudo npm install @vercel/node
  test:
    docker:
      - image: circleci/node:16
    steps:
      - checkout
      # - run:
      #     name: Run Tests
      #     command: sudo npm test #change test in package.json 
      - run:
          name: Hello World!
          command: echo "Hello, World!"

  deploy:
    docker:
      - image: circleci/node:16
    steps:
      - checkout
      - run:
          name: Install Vercel CLI
          command: sudo npm install --global vercel@latest
      - run:
          name: Pull
          command: vercel pull --yes --environment=production --token=$VERCEL_TOKEN
      - run:
          name: Build 
          command: vercel build --token $VERCEL_TOKEN --prod
      - run:
          name: Deploy
          command: vercel deploy --prebuilt --prod --token=$VERCEL_TOKEN

workflows:
  version: 2
  build_test_deploy:
    jobs:
      # - build
      - test
      - deploy:
          requires:
            - test
