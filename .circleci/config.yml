version: 2.1

jobs:
  # build:
  #   docker:
  #     - image: circleci/node:16
  #   steps:
  #     - checkout
  #     - run:
  #         name: Install Dependencies
  #         command: sudo npm install
  test:
    docker:
      - image: circleci/node:16
    steps:
      - checkout
      # - run:
      #     name: Run Tests
      #     command: sudo npm test #change test in package.json 
      - run:
          name: Hello World!
          command: echo "Hello, World!"

  deploy:
    docker:
      - image: circleci/node:16
    steps:
      - checkout
      - run:
          name: Install Vercel CLI
          command: sudo npm install --global vercel@latest
      - run:
          name: Deploy to Vercel
          command: vercel --token $VERCEL_TOKEN --prod nodejs18.x --yes

workflows:
  version: 2
  build_test_deploy:
    jobs:
      # - build
      - test
      - deploy:
          requires:
            - test
